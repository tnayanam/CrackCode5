/*
 * Rotate Matrix O(N Square)
 *
 */

#include <iostream>
#include <string>
#include <conio.h>
using namespace std;
int main()
{
	int matrix[4][4] = { 0, 2, 3, 4, 5, 6, 0, 8, 9, 10, 11, 12, 0, 14, 15, 16 };
	int row[4] = { 0 };
	int column[4] = { 0 };
	int temp;
	for (int i = 0; i < 4; i++)
	{
		for (int j = 0; j < 4; j++)
		{
			cout << matrix[i][j] << " ";
		}
		cout << "\n";
	}
	
	for (int i = 0; i < 4; i++)
	{
		for (int j = 0; j < 4; j++)
		{
			if (matrix[i][j] == 0)
			{
				row[i] = 1;
				column[j] = 1;
			}
		}
	}

	for (int i = 0; i < 4; i++) // traversing the row array
	{
		for (int j = 0; j < 4; j++)
		{
			if (row[i] == 1 || column[j] == 1)
				matrix[i][j] = 0;
		}
	}

	for (int i = 0; i < 4; i++)
	{
		for (int j = 0; j < 4; j++)
		{
			cout << matrix[i][j] << " ";
		}
		cout << "\n";
	}
	
	_getch();

}